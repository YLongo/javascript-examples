<!DOCTYPE html>

<html lang="zh-cn">

    <head>
        <meta charset="utf-8" />
        <title>个人信息查询</title>
    </head>
    <body>
        {{ if name}}
        <h2>{{name}}的个人信息</h2>
        <table>
            <tr>
                <td>姓名</td>
                <td>{{name}}</td>
            </tr>
            <tr>
                <td>年龄</td>
                <td>{{age}}</td>
            </tr>
            <tr>
                <td>性别</td>
                <td>{{sex}}</td>
            </tr>
            <tr>
                <td>爱好</td>
                <td>
                    {{ each items}} 
                        {{ $value }} 
                    {{ /each }}
                </td>
        </table>
        {{ else }}
        <h2>没有找到该用户</h2>
        {{ /if }}
    </body>
</html>